counts={"Chuck":1,"Fred":42,"Jan":100}  #First we create a dictionary.
for key in counts:  #We initiate a for loop.
    print(key,counts[key])  #If we wanted just the keys of the dictionary(we would have typed: print(key))
                            #If we wanted just the values, we would have typed: print(counts[key])
                            #Lastly, with print(key,counts[key]) we print out both.
